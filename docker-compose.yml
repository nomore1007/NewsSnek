services:
  news-reader:
    build:
      context: .
      dockerfile: Dockerfile
      pull: true
    container_name: news-reader
    volumes:
      - ./data:/app/data
    environment:
      - INTERVAL=${INTERVAL}
      - PYTHONPATH=/app
    restart: unless-stopped
    command: ["python3", "nwsreader.py", "--file", "sources.txt", "--overview"]